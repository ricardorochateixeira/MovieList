<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ricardoteixeira.movielist">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.ricardoteixeira.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.ricardoteixeira.presentation.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.ricardoteixeira.presentation.addmovie.AddMovieActivity"
            android:label="MovieList">
        </activity>

        <activity
            android:name="com.ricardoteixeira.presentation.searchmovie.SearchMovieActivity"
            android:label="SearchMovie">
        </activity>
    </application>

</manifest>